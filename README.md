# Jupyter Notebook

## Install
https://jupyter.org/install

```zsh
cd <directory>
python3 -m venv venv
source venv/bin/activate # should see (venv) 
pip install jupyter
pip install plotly
pip install numpy # needed?
jupyter notebook
```